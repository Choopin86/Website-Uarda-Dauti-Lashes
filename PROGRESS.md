# Progress — Uarda Dauti Lashes Website

_Last updated: 2026-07-21_

## Current state

Homepage, services page, and About page are built, run the full JSON → transform → render pipeline, and are verified in-browser (no console errors, production build passes). Live at `www.uardadautilashes.com`. `contact.html` build is underway: shared Header, Contact Information, and Contact Form sections are wired and styled; Map section is still a placeholder, and the Contact Form doesn't yet send anywhere (client-side validation only). `about.html` is complete against `Documentation/About Us Page Layout.md` — structural build, styling pass, and the Business Story expand/collapse interaction are all done and verified in-browser in both languages, including mobile widths (900px grid-collapse breakpoint added to Page Header/Business Story; Achievements section given its own dark-background/title-color treatment scoped to `.achievements-section`).

## Done

- Homepage — all 9 sections wired and verified.
- Services page — all 5 sections wired and verified (intro banner, full services list, detail dialog, booking CTA row, footer). Both "Book now" buttons (service card and detail dialog) open the Instagram DM link (`contactInformation.json`'s `book-link`) directly. The bottom Booking CTA section is the full icon set (phone/WhatsApp/Instagram/TikTok), general-purpose, not scoped to a service. `Documentation/Services Page Layout.md` is up to date with this behavior.
- Deployed to Vercel at the custom domain.
- Language toggle — implemented in `src/utils/language.js`, wired into both `home.js` and `services.js`; all data is already bilingual.
- About page — structural build complete: Page Header, Business Story (Bio), Achievements (reused from homepage), shared Header/Footer orchestrator wiring. Verified in both languages, no console errors. Salon Environment section was dropped per client decision (removed from doc, markup, and progress notes — no src code had been written for it).
- About page styling — complete. Page Header (`.about-header`) and Business Story (`.business-story`/`.portrait-container`/`.bio-container`) sections: circular portrait frames (new `--radius-circle` token), side-by-side grid layouts with a portrait-first column order (owner's deliberate choice, differs from the homepage hero's text-first order), full-bleed section backgrounds with a centered inner content wrapper (matching the `.trust-strip`/`.trust-items` pattern). New files: `src/styles/sections/about-header.css`, `business-story.css`. Achievements section (`.achievements-section`, reusing homepage's `renderAchievements`) got its own dark-background/padding and `.section-title`/`.section-sub` color overrides added to the existing `achievements.css`, mirroring the homepage's `.achievements-preview` treatment — no new file needed since the inner slider styles are already unscoped and shared. Both grid sections also got a `900px` mobile breakpoint (collapses to one column, caps portrait width at 320px) since they were the only two-column grid sections in the codebase missing one; verified in-browser at 1280px/900px/375px/320px and in both languages.
- Business Story "expand to view longBio" interaction — implemented and verified against `Documentation/About Us Page Layout.md` section 2. Clicking "Read more" swaps the displayed paragraphs from `shortBio` to `longBio` (a fuller rewrite, not a continuation, so it replaces rather than appends); "Read less" swaps back. New `src/data/aboutCopy.json` holds the bilingual `readMore`/`readLess` button labels, following the same per-page `*Copy.json` convention as `servicesCopy.json`. `transformAboutBio.js` now takes a `copy` param and returns `longBio` + `labels`; `renderAboutBio.js` owns the toggle state locally (a closure `expanded` boolean) and swaps the button's `textContent`/`aria-expanded`. New `.bio-toggle` style in `business-story.css` (text-link style, not `.btn-pill`, per owner's call — a pill read as too strong a CTA for a "show more text" affordance). Verified in-browser in both languages and at 1280px/375px — toggle state resets to collapsed on language switch, consistent with the rest of the page's full-rebuild-on-toggle behavior.
- Footer contrast fix — `.footer` (`src/styles/sections/footer.css`, shared across all pages) now has `border-top: 1px solid var(--accent)`. Without it, a dark section immediately followed by the (also dark `--ink`) footer read as one merged block — most visible on the About page's Achievements→Footer boundary, less so on Home's CTA→Footer boundary since the floating cream CTA card there already implied an edge. A subtler `--ink-soft` border (matching `.footer-copyright`'s existing internal divider) was tried first and rejected by the owner as insufficient contrast; the accent-colored border was the accepted fix and doubles as reinforcement of the brand's warm accent color already used for rings/badges elsewhere.
- Contact page — shared Header wired (`src/components/contact.js`). Contact Information section (`.contact-information`) structurally built and verified in-browser in both languages, no console errors: phone/email render as real `tel:`/`mailto:` deep links (a deliberate upgrade over the footer's plain-text phone/email), address/opening hours are plain text with no map link (Map section owns that, per `Documentation/Contact Us Page Layout.md`'s "must not duplicate form/map behavior" constraint), and social links reuse the footer's anchor pattern. New `src/data/contactCopy.json` holds a drafted bilingual eyebrow/title heading for the section. No CSS yet — structural build only, following the About page's precedent of separating structure from styling.
- Contact Form section (`.contact-form-section`) — structurally built and verified in-browser: Full Name/Email/Full Address/Phone Number/Message fields plus a required consent checkbox, all per `FormModel` in `Documentation/Architecture.md`. New `transformContactForm.js`/`renderContactForm.js`, and a `form` key added to `contactCopy.json` (instructions, field labels, consent text, submit label, error/success messages — also drafted, needs owner review). Submission is intercepted client-side (`preventDefault`, `noValidate` on the form) and validated: blocks with a translated error if any required field is empty, blocks separately if consent isn't checked, otherwise shows a success message and resets the form. **Deliberately not wired to a backend yet** — owner chose "UI + validation only" for this pass since the site is static and picking a delivery method (hosted form service vs. a Vercel serverless function + email API) is a separate decision with its own account/env-var setup.
- Contact page styling — complete for Contact Information and Contact Form (Header is shared/already styled). New `src/styles/sections/contact-information.css`, `contact-form.css`. Contact Information reuses the homepage Trust-strip's icon-row-on-cream rhythm (same 52px accent-outlined circle motif) rather than inventing a new layout; phone/email icon circles fill solid accent on hover via `:has()` (already used in `featured.css`) while address/hours circles stay static outline — a wordless signal for which rows are actually clickable. Contact Form sits on ink for contrast, with underline-only inputs (no boxed borders, matching the site's restraint) and italic Cormorant Garamond instructions text as the section's one signature flourish. Submit button upgraded to `btn-pill btn-pill--solid` (matching the "Book Now" CTA convention elsewhere) with an `--ink` text-color override for contrast on the accent fill. `renderContactForm.js`'s status message now includes a Font Awesome icon (circle-check/circle-exclamation) to distinguish success from error without introducing a new color outside the existing token set. Verified in-browser in both languages at 1024px/375px widths (iframe technique, since Chrome runs fullscreen) — no console errors, `npm run build` passes, homepage re-checked for regressions.

## Pending (suggested order)

1. **Contact page** — wire the Contact Form to a real backend (owner still needs to choose: a hosted form service like Formspree/Web3Forms, or a Vercel serverless function + email API) and build the Maps embed.
2. **Copy review (owner)** — drafted copy lives in `src/data/trustHighlights.json`, `homeCopy.json`, `servicesCopy.json`, `businessIdentity.json`'s `servicesIntroText`, and `contactCopy.json`'s `information` + `form` keys. Edit those JSON files directly.
3. **Media assets** — `media.json` references `media1`–`media16` but only a few have real files; add photos/videos to `public/media/` to light up remaining cards. (`portrait.jpeg` was added earlier for the About page's bio/header images.)
4. **Real-device mobile check** — ongoing; owner is checking the live site on their own smartphone regularly.

## Non-obvious decisions worth knowing

- Featured homepage service is Professional Lash Courses (`service8`) — deliberate pick, it's the differentiator and targets a second audience (students).
- Missing `media.json` refs are skipped, never fatal — intentional (data is incomplete by design right now), not a bug to "fix" by adding fallback images.
- Copy in the JSON files listed under Pending #3 was Claude-drafted from `bio.json` facts only (no invented marketing claims) and still needs owner sign-off.
- About page's structural build was deliberately finished before any styling — owner's call, to keep the JSON→transform→render build separate from the CSS pass. (The About page was originally built with Claude guiding step-by-step and the owner writing the code, as a learning exercise; that workflow has since ended — Claude now writes code directly like on the rest of the site.)
- Full-bleed section backgrounds require the `max-width`/`margin: 0 auto` centering to live on a separate inner wrapper, never on the same element as the section's `background-color` — otherwise the background gets clipped to the centered content width. Found and fixed on both `.about-header` and `.business-story` this session; worth checking for on any new section.
- Renderers must assign a `className` to every element they create (even plain text nodes like bio paragraphs) — `renderAboutHeader.js` and `renderAboutBio.js` were missing this and had to be retrofitted; every other renderer (e.g. `renderHero.js`, `renderTrust.js`) already follows this convention.
